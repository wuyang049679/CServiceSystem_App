package com.hc_android.hc_css.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import java.util.List;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CACHE_BEAN".
*/
public class CacheBeanDao extends AbstractDao<CacheBean, Long> {

    public static final String TABLENAME = "CACHE_BEAN";

    /**
     * Properties of entity CacheBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property KeyId = new Property(0, Long.class, "keyId", true, "_id");
        public final static Property DId = new Property(1, String.class, "dId", false, "D_ID");
        public final static Property BeanList = new Property(2, String.class, "beanList", false, "BEAN_LIST");
    }

    private final SubCachedBeanConverter beanListConverter = new SubCachedBeanConverter();

    public CacheBeanDao(DaoConfig config) {
        super(config);
    }
    
    public CacheBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CACHE_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: keyId
                "\"D_ID\" TEXT UNIQUE ," + // 1: dId
                "\"BEAN_LIST\" TEXT);"); // 2: beanList
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CACHE_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CacheBean entity) {
        stmt.clearBindings();
 
        Long keyId = entity.getKeyId();
        if (keyId != null) {
            stmt.bindLong(1, keyId);
        }
 
        String dId = entity.getDId();
        if (dId != null) {
            stmt.bindString(2, dId);
        }
 
        List beanList = entity.getBeanList();
        if (beanList != null) {
            stmt.bindString(3, beanListConverter.convertToDatabaseValue(beanList));
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CacheBean entity) {
        stmt.clearBindings();
 
        Long keyId = entity.getKeyId();
        if (keyId != null) {
            stmt.bindLong(1, keyId);
        }
 
        String dId = entity.getDId();
        if (dId != null) {
            stmt.bindString(2, dId);
        }
 
        List beanList = entity.getBeanList();
        if (beanList != null) {
            stmt.bindString(3, beanListConverter.convertToDatabaseValue(beanList));
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CacheBean readEntity(Cursor cursor, int offset) {
        CacheBean entity = new CacheBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // keyId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // dId
            cursor.isNull(offset + 2) ? null : beanListConverter.convertToEntityProperty(cursor.getString(offset + 2)) // beanList
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CacheBean entity, int offset) {
        entity.setKeyId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setBeanList(cursor.isNull(offset + 2) ? null : beanListConverter.convertToEntityProperty(cursor.getString(offset + 2)));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CacheBean entity, long rowId) {
        entity.setKeyId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CacheBean entity) {
        if(entity != null) {
            return entity.getKeyId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CacheBean entity) {
        return entity.getKeyId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
